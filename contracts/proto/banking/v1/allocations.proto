syntax = "proto3";

package banking.v1;

// AllocationsService — distribution of company and user capital inside the bank
// and across external services.
//
// Allocation moves are cross-boundary sagas over TigerBeetle two-phase transfers.
// Scaffold: declared to lock the namespace; RPCs land here as the allocations
// feature arrives. See docs/ARCHITECTURE.md.
service AllocationsService {}
