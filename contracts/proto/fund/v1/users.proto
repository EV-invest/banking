syntax = "proto3";

package fund.v1;

// UsersService — investor accounts and their investments.
//
// Scaffold: declared to lock the namespace; RPCs land here as the users feature
// arrives. See docs/ARCHITECTURE.md.
service UsersService {}
