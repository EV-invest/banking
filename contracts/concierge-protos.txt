# Concierge protos banking vendors through the pinned `evconcierge_contracts` git dep.
#
# The ONE list read by both:
#   - `nix run .#gen-api` (flake.nix, runGenApi): every file here is fed to protoc
#     alongside contracts/proto/banking/v1/*.proto to produce contracts/openapi.json
#     and the cabinet's shared/contracts/gen types;
#   - `nix run .#concierge-pin-check` (contracts/concierge-pin-check.sh): every file
#     here has its bytes compared pin ↔ concierge origin/main ↔ local cargo checkout.
#
# Add a proto here when the cabinet starts consuming it; it is then generated AND
# guarded in one move (issue #293: governance.proto was generated but not guarded).
# Paths are relative to concierge's contracts/proto/. One per line; `#` comments.
concierge/v1/directory.proto
concierge/v1/auth.proto
concierge/v1/governance.proto
