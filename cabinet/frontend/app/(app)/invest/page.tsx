// Invest is intentionally blank for now — the fund-shares surface (positions · subscribe ·
// redeem) is parked while the screen is redesigned. The route + app shell stay so the nav
// keeps working; the implementation lives in views/invest/ui/invest-view.tsx for later.
export default function InvestPage() {
  return <div className="px-8 pt-6" />;
}
