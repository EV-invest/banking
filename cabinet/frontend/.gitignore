# Next.js build output (also covered at the repo root)
.next
/out/
next-env.d.ts

# Built element-remote bundles (regenerated by `npm run build:mfe`, run on predev/prebuild)
/public/mfe/

# Env
.env
.env.local

# OS / editor
.DS_Store
*.tsbuildinfo
.eslintcache
