{
  "name": "ev-banking",
  "private": true,
  "description": "EV banking hub — Turborepo of client apps (clients/*). The Rust hub (domain, contracts, piggybank/{core,auth}) lives alongside in the Cargo workspace.",
  "workspaces": [
    "clients/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
